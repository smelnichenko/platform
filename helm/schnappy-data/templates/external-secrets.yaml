{{- if .Values.vault.secretsEnabled }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.postgres.secretName" . }}
  labels:
    {{- include "schnappy.postgres.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.postgres.secretName" . }}
  data:
    - secretKey: POSTGRES_DB
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: database
    - secretKey: POSTGRES_USER
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: username
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: password
{{- range .Values.postgres.databases }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.fullname" $ }}-postgres-{{ .name }}
  labels:
    {{- include "schnappy.postgres.labels" $ | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.fullname" $ }}-postgres-{{ .name }}
  data:
    - secretKey: DB_NAME
      remoteRef:
        key: {{ $.Values.vault.secretPathPrefix }}/postgres-{{ .name }}
        property: database
    - secretKey: DB_USERNAME
      remoteRef:
        key: {{ $.Values.vault.secretPathPrefix }}/postgres-{{ .name }}
        property: username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: {{ $.Values.vault.secretPathPrefix }}/postgres-{{ .name }}
        property: password
{{- end }}
{{- if or .Values.valkey.password .Values.valkey.existingSecret }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.valkey.secretName" . }}
  labels:
    {{- include "schnappy.valkey.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.valkey.secretName" . }}
  data:
    - secretKey: VALKEY_PASSWORD
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/valkey
        property: password
{{- end }}
{{- /* Shared S3 credentials secret for the in-cluster s3gw (versitygw) — consumed by
       the s3gw server, the bucket-creation Job, Mimir and Tempo. Neutral
       ACCESS_KEY/SECRET_KEY keys; the Vault properties stay access_key/secret_key. */}}
{{- if eq (.Values.s3gw.enabled | toString) "true" }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.s3gw.secretName" . }}
  labels:
    {{- include "schnappy.s3gw.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.s3gw.secretName" . }}
  data:
    - secretKey: ACCESS_KEY
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/minio
        property: access_key
    - secretKey: SECRET_KEY
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/minio
        property: secret_key
{{- end }}
{{- if eq (.Values.strimzi.enabled | toString) "true" }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.kafka.secretName" . }}
  labels:
    {{- include "schnappy.kafka.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.kafka.secretName" . }}
  data:
    - secretKey: KAFKA_CLUSTER_ID
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/kafka
        property: cluster_id
{{- end }}
{{- end }}
