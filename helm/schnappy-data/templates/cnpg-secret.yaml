{{- if and (eq (.Values.cnpg.enabled | toString) "true") .Values.vault.secretsEnabled }}
---
# CNPG superuser secret (postgres user, password from Vault)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.postgres.serviceName" . }}-superuser
  labels:
    {{- include "schnappy.postgres.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.postgres.serviceName" . }}-superuser
    template:
      data:
        username: postgres
        password: "{{ "{{ .password }}" }}"
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: password
---
# CNPG app user secret (monitor user, credentials from Vault)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.postgres.serviceName" . }}-app
  labels:
    {{- include "schnappy.postgres.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.postgres.serviceName" . }}-app
  data:
    - secretKey: username
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: username
    - secretKey: password
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/postgres
        property: password
{{- if .Values.cnpg.backup }}
---
# CNPG backup S3 credentials (Pi MinIO)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.cnpg.backupSecret }}
  labels:
    {{- include "schnappy.postgres.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ .Values.cnpg.backupSecret }}
  data:
    - secretKey: MINIO_ROOT_USER
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/minio-backup
        property: access_key
    - secretKey: MINIO_ROOT_PASSWORD
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/minio-backup
        property: secret_key
{{- end }}
{{- end }}
