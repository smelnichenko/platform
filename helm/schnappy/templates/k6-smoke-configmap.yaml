{{- if eq (.Values.smokeTest.enabled | toString) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: schnappy-k6-smoke
data:
  # Kept byte-identical with the other chart's files/smoke-test.js (CI cmp).
  smoke-test.js: |
{{ .Files.Get "files/smoke-test.js" | indent 4 }}
{{- end }}
