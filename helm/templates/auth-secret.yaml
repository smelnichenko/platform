{{- if and .Values.auth.jwtSecret (not .Values.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "schnappy.auth.secretName" . }}
  labels:
    {{- include "schnappy.monitor.labels" . | nindent 4 }}
type: Opaque
stringData:
  JWT_SECRET: {{ .Values.auth.jwtSecret | quote }}
{{- end }}
