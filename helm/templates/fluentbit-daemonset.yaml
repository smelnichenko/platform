{{- if eq (.Values.elk.enabled | toString) "true" }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "monitor.fullname" . }}-fluentbit
  labels:
    {{- include "monitor.fluentbit.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "monitor.fluentbit.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "monitor.fluentbit.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/fluentbit-configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "monitor.fullname" . }}-fluentbit
      securityContext:
        runAsUser: 0
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: fluent-bit
          image: {{ .Values.elk.fluentbit.image }}
          ports:
{{- if eq (.Values.elk.ciLogs.enabled | toString) "true" }}
            - name: forward
              containerPort: 24224
              hostPort: 24224
              protocol: TCP
{{- end }}
          env:
            - name: ELASTICSEARCH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "monitor.elasticsearch.secretName" . }}
                  key: ELASTICSEARCH_PASSWORD
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - name: config
              mountPath: /fluent-bit/etc/fluent-bit.conf
              subPath: fluent-bit.conf
            - name: parsers
              mountPath: /fluent-bit/etc/parsers.conf
              subPath: parsers.conf
            - name: varlogcontainers
              mountPath: /var/log/containers
              readOnly: true
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
            - name: db
              mountPath: /fluent-bit/db
          resources:
            {{- toYaml .Values.elk.fluentbit.resources | nindent 12 }}
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - DAC_READ_SEARCH
      volumes:
        - name: config
          configMap:
            name: {{ include "monitor.fullname" . }}-fluentbit-config
        - name: parsers
          configMap:
            name: {{ include "monitor.fullname" . }}-fluentbit-config
        - name: varlogcontainers
          hostPath:
            path: /var/log/containers
        - name: varlogpods
          hostPath:
            path: /var/log/pods
        - name: db
          emptyDir: {}
      tolerations:
        - effect: NoSchedule
          operator: Exists
{{- end }}
