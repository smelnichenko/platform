{{- if and (eq (.Values.kafka.enabled | toString) "true") (not .Values.kafka.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "monitor.kafka.secretName" . }}
  labels:
    {{- include "monitor.kafka.labels" . | nindent 4 }}
type: Opaque
data:
  KAFKA_CLUSTER_ID: {{ .Values.kafka.clusterId | b64enc | quote }}
{{- end }}
