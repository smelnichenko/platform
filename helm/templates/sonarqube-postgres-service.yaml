{{- if eq (.Values.sonarqube.enabled | toString) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "schnappy.sonarqube.postgres.serviceName" . }}
  labels:
    {{- include "schnappy.sonarqube.postgres.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "schnappy.sonarqube.postgres.selectorLabels" . | nindent 4 }}
{{- end }}
