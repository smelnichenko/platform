{{- if and .Values.ai.apiKey (not .Values.ai.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "monitor.ai.secretName" . }}
  labels:
    {{- include "monitor.app.labels" . | nindent 4 }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: {{ .Values.ai.apiKey | quote }}
{{- end }}
