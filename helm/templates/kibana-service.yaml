{{- if eq (.Values.elk.enabled | toString) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "schnappy.kibana.serviceName" . }}
  labels:
    {{- include "schnappy.kibana.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5601
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "schnappy.kibana.selectorLabels" . | nindent 4 }}
{{- end }}
