{{- if .Values.postgres.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor.postgres.serviceName" . }}
  labels:
    {{- include "monitor.postgres.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    {{- include "monitor.postgres.selectorLabels" . | nindent 4 }}
{{- end }}
