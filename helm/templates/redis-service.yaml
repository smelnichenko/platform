{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "schnappy.redis.serviceName" . }}
  labels:
    {{- include "schnappy.redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    {{- include "schnappy.redis.selectorLabels" . | nindent 4 }}
{{- end }}
