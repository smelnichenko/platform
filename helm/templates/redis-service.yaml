{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor.redis.serviceName" . }}
  labels:
    {{- include "monitor.redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    {{- include "monitor.redis.selectorLabels" . | nindent 4 }}
{{- end }}
