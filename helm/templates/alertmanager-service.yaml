{{- if and .Values.prometheus.enabled (eq (.Values.alertmanager.enabled | toString) "true") -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor.alertmanager.serviceName" . }}
  labels:
    {{- include "monitor.alertmanager.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 9093
      targetPort: 9093
      protocol: TCP
      name: http
  selector:
    {{- include "monitor.alertmanager.selectorLabels" . | nindent 4 }}
{{- end }}
