{{- if and .Values.victoriametrics.enabled (eq (.Values.kubeStateMetrics.enabled | toString) "true") -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "schnappy.kubeStateMetrics.serviceName" . }}
  labels:
    {{- include "schnappy.kubeStateMetrics.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: metrics
  selector:
    {{- include "schnappy.kubeStateMetrics.selectorLabels" . | nindent 4 }}
{{- end }}
