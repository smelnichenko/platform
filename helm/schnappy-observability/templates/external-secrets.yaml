{{- if .Values.vault.secretsEnabled }}
{{- if and .Values.grafana.enabled .Values.grafana.existingSecret }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.grafana.secretName" . }}
  labels:
    {{- include "schnappy.grafana.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.grafana.secretName" . }}
  data:
    - secretKey: admin-user
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/grafana
        property: admin_user
    - secretKey: admin-password
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/grafana
        property: admin_password
    {{- if eq (.Values.grafana.oauth.enabled | toString) "true" }}
    - secretKey: oauth-client-id
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/grafana
        property: oauth_client_id
    - secretKey: oauth-client-secret
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/grafana
        property: oauth_client_secret
    {{- end }}
{{- end }}
{{- if and .Values.vault.secretsEnabled (eq (.Values.clickhouse.enabled | toString) "true") .Values.clickhouse.existingSecret }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.clickhouse.secretName" . }}
  labels:
    {{- include "schnappy.clickhouse.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.clickhouse.secretName" . }}
  data:
    - secretKey: CLICKHOUSE_PASSWORD
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/clickhouse
        property: password
{{- end }}
{{- if eq (.Values.alertmanager.enabled | toString) "true" }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "schnappy.alertmanager.secretName" . }}
  labels:
    {{- include "schnappy.alertmanager.labels" . | nindent 4 }}
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "schnappy.alertmanager.secretName" . }}
  data:
    - secretKey: ALERTMANAGER_WEBHOOK_SECRET
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/alertmanager
        property: webhook_secret
    - secretKey: SMTP_PASSWORD
      remoteRef:
        key: {{ .Values.vault.secretPathPrefix }}/mail
        property: password
{{- end }}
{{- end }}
