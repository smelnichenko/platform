{{- if and (eq (.Values.prometheus.enabled | toString) "true") (eq (.Values.alertmanager.enabled | toString) "true") }}
{{- $fn := include "schnappy.fullname" . }}
{{- $ns := .Release.Namespace }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ $fn }}-alerts
  labels:
    {{- include "schnappy.labels" . | nindent 4 }}
spec:
  groups:
    - name: infra-watchdog
      rules:
        - alert: Watchdog
          expr: vector(1)
          labels:
            severity: none
          annotations:
            runbook_url: "https://runbooks.pmon.dev/Watchdog"
            summary: "Watchdog alert (always firing)"
            description: "Dead man's switch. If not firing, the alerting pipeline is broken."

    - name: infra-logs
      rules:
        - alert: FluentbitDown
          expr: kube_pod_status_ready{namespace="{{ $ns }}", condition="true", pod=~"{{ $fn }}-fluentbit-.*"} == 0
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/FluentbitDown"
            summary: "Fluent-bit {{ "{{" }} $labels.pod {{ "}}" }} is down"

    - name: infra-certificates
      rules:
        - alert: CertExpiringIn30Days
          expr: certmanager_certificate_expiration_timestamp_seconds - time() < 30 * 24 * 3600
          for: 1h
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/CertExpiringIn30Days"
            summary: "Certificate {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.name {{ "}}" }} expiring in less than 30 days"
        - alert: CertExpiringIn7Days
          expr: certmanager_certificate_expiration_timestamp_seconds - time() < 7 * 24 * 3600
          for: 30m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/CertExpiringIn7Days"
            summary: "Certificate {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.name {{ "}}" }} expiring in less than 7 days"

    - name: infra-pvc
      rules:
        - alert: PVCUsageHigh
          expr: kubelet_volume_stats_used_bytes{namespace="{{ $ns }}"} / kubelet_volume_stats_capacity_bytes{namespace="{{ $ns }}"} > 0.8
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PVCUsageHigh"
            summary: "PVC {{ "{{" }} $labels.persistentvolumeclaim {{ "}}" }} usage high"
        - alert: PVCUsageCritical
          expr: kubelet_volume_stats_used_bytes{namespace="{{ $ns }}"} / kubelet_volume_stats_capacity_bytes{namespace="{{ $ns }}"} > 0.9
          for: 5m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PVCUsageCritical"
            summary: "PVC {{ "{{" }} $labels.persistentvolumeclaim {{ "}}" }} usage critical"

    - name: infra-vault
      rules:
        - alert: VaultSealed
          expr: probe_http_status_code{job="blackbox-vault"} == 503 or probe_success{job="blackbox-vault"} == 0
          for: 2m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/VaultSealed"
            summary: "Vault at {{ "{{" }} $labels.instance {{ "}}" }} is sealed or unreachable"
            description: "Pi Vault returned 503 (sealed) or was unreachable for 2m. ESO will fail to refresh any ExternalSecret."

    - name: infra-backup-store
      rules:
        - alert: VersitygwGatewayDown
          # versitygw runs on BOTH Pis (stateless, no consul lock); the VIP
          # routes to a healthy one. A per-Pi :9000/health failing = that gateway
          # is down (lost redundancy; if both are down the backup store is down).
          # The `or absent(...)` arm fires when the probe stops producing series
          # entirely (blackbox down, Probe CR gone) — without it, count()-over-
          # empty is silent exactly when the store is unmonitored.
          expr: count(probe_success{job="blackbox-versitygw"} == 0) > 0 or absent(probe_success{job="blackbox-versitygw"})
          for: 5m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/VersitygwGatewayDown"
            summary: "A versitygw backup-store gateway is down (or its probe is not reporting)"
            description: "A per-Pi versitygw :9000/health probe is failing, or the probe has stopped reporting entirely. The VIP routes to a healthy gateway; if both gateways are down the backup store (velero/etcd/CNPG/scylla) is unavailable."

    - name: infra-eso
      rules:
        - alert: ClusterSecretStoreNotReady
          expr: external_secrets_cluster_secret_store_condition{condition="Ready", status="true"} == 0
          for: 5m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/ClusterSecretStoreNotReady"
            summary: "ClusterSecretStore {{ "{{" }} $labels.name {{ "}}" }} not Ready"
            description: "ESO cannot reach its backing store. All ExternalSecrets sourced from it will stop refreshing."
        - alert: ExternalSecretSyncFailing
          expr: sum by (namespace, name) (rate(externalsecret_sync_calls_error[10m])) > 0
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/ExternalSecretSyncFailing"
            summary: "ExternalSecret {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.name {{ "}}" }} sync failing"

    - name: infra-argocd
      rules:
        - alert: ArgoCDAppDegraded
          expr: argocd_app_info{health_status="Degraded"} == 1
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/ArgoCDAppDegraded"
            summary: "ArgoCD app {{ "{{" }} $labels.name {{ "}}" }} Degraded"
            description: "Application has been Degraded for 15m. Check resource tree for the failing child."
        - alert: ArgoCDAppOutOfSync
          expr: argocd_app_info{sync_status!="Synced"} == 1
          for: 30m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/ArgoCDAppOutOfSync"
            summary: "ArgoCD app {{ "{{" }} $labels.name {{ "}}" }} OutOfSync 30m+"
        - alert: ArgoCDAppSyncFailed
          # A failing sync operation (or a sync hook, e.g. the k6 smoke test) leaves
          # the app reporting Synced/Healthy, so ArgoCDAppOutOfSync/Degraded never
          # catch it. This watches the sync-operation result counter instead.
          expr: sum by (name) (increase(argocd_app_sync_total{phase=~"Failed|Error"}[1h])) > 0
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/ArgoCDAppOutOfSync"
            summary: "ArgoCD app {{ "{{" }} $labels.name {{ "}}" }} sync operation failing"
            description: "A sync (or one of its hooks, e.g. the k6 smoke test) failed in the last hour while the app still shows Synced/Healthy."

    # kube-state-metrics workload-health alerts. These depend on KSM being scraped
    # (kube-state-metrics-servicemonitor.yaml). KSM emits the condition metrics
    # kube_job_failed / kube_pod_container_status_waiting_reason only while the
    # condition is active (they read empty when nothing is failing/waiting), so these
    # rules derive equivalent signals from the always-present kube_job_status_*
    # counters and container restart counts.
    - name: infra-kube-state
      rules:
        - alert: KubeJobFailed
          # "Genuinely failed" = has failed pods, never succeeded, and nothing still
          # running (retries exhausted). Excludes jobs that retried then succeeded,
          # and in-progress jobs. Catches the k6 smoke-test PostSync hook, etc.
          # The unless joins are pinned on(namespace, job_name): kube_job_status_failed
          # carries a `reason` label (e.g. BackoffLimitExceeded) that the succeeded/
          # active series lack, so a bare unless would match nothing and suppress nothing.
          expr: (kube_job_status_failed > 0) unless on(namespace, job_name) (kube_job_status_succeeded > 0) unless on(namespace, job_name) (kube_job_status_active > 0)
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubeJobFailed"
            summary: "Job {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.job_name {{ "}}" }} failed"
            description: "A Job exhausted its retries without completing. Check the job's pod logs."
        - alert: KubePodCrashLooping
          # kube_pod_container_status_waiting_reason is emitted only while a container
          # is waiting; detect crash-loops via restart rate instead — a container
          # restarting more than 5 times in an hour — independent of catching that
          # transient waiting-reason series at scrape time.
          expr: increase(kube_pod_container_status_restarts_total[1h]) > 5
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubePodCrashLooping"
            summary: "Pod {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.pod {{ "}}" }} crash-looping"
            description: "Container {{ "{{" }} $labels.container {{ "}}" }} restarted >5 times in the last hour."
        - alert: KubePodNotReady
          # Scoped to Pending|Unknown only — the textbook expr also matches Failed phase,
          # which floods on terminal/Error pods shadowed by a healthy replica. A pod
          # stuck on ImagePullBackOff / scheduling sits in Pending and is still caught.
          expr: kube_pod_status_phase{phase=~"Pending|Unknown"} == 1
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubePodNotReady"
            summary: "Pod {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.pod {{ "}}" }} not ready ({{ "{{" }} $labels.phase {{ "}}" }})"
            description: "Pod has been Pending/Unknown for 15m. Check scheduling, image pulls, init containers."
        - alert: KubeDeploymentReplicasMismatch
          # changes(...)==0 guard avoids firing during a normal rollout where the
          # available count is still moving toward spec.
          expr: |
            (kube_deployment_spec_replicas - kube_deployment_status_replicas_available) > 0
              and
            changes(kube_deployment_status_replicas_available[10m]) == 0
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubeDeploymentReplicasMismatch"
            summary: "Deployment {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.deployment {{ "}}" }} replicas mismatch"
            description: "Available replicas have not matched desired for 15m and stopped progressing."
        - alert: KubeStatefulSetReplicasMismatch
          expr: |
            (kube_statefulset_replicas - kube_statefulset_status_replicas_ready) > 0
              and
            changes(kube_statefulset_status_replicas_ready[10m]) == 0
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubeStatefulSetReplicasMismatch"
            summary: "StatefulSet {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.statefulset {{ "}}" }} replicas mismatch"
            description: "Ready replicas have not matched desired for 15m and stopped progressing."
        - alert: KubeDaemonSetUnavailable
          expr: kube_daemonset_status_number_unavailable > 0
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/KubeDaemonSetUnavailable"
            summary: "DaemonSet {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.daemonset {{ "}}" }} has unavailable pods"
            description: "One or more DaemonSet pods have been unavailable for 15m."

    - name: infra-velero
      rules:
        - alert: VeleroBSLUnavailable
          expr: velero_backup_last_status{schedule!=""} == 0 or absent(velero_backup_last_successful_timestamp{schedule="velero-schnappy-daily"} > time() - 30 * 3600)
          for: 10m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/VeleroBSLUnavailable"
            summary: "Velero daily backup missing or failed"
            description: "No successful schnappy-daily backup in the last 30h."
        - alert: VeleroBackupFailing
          expr: increase(velero_backup_failure_total[1h]) > 0
          for: 5m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/VeleroBackupFailing"
            summary: "Velero backup failed"

    - name: infra-public-urls
      rules:
        - alert: PublicEndpointDown
          expr: probe_success{job="blackbox-public"} == 0
          for: 3m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PublicEndpointDown"
            summary: "Public URL {{ "{{" }} $labels.instance {{ "}}" }} is down"
            description: "Blackbox probe failed for 3m. Check gateway, route binding, upstream."
        - alert: PublicEndpointSlow
          expr: probe_duration_seconds{job="blackbox-public"} > 3
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PublicEndpointSlow"
            summary: "Public URL {{ "{{" }} $labels.instance {{ "}}" }} slow"
            description: "End-to-end probe > 3s for 10m."
        - alert: PublicSSLCertExpiringSoon
          expr: (probe_ssl_earliest_cert_expiry{job="blackbox-public"} - time()) / 86400 < 14
          for: 1h
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PublicSSLCertExpiringSoon"
            summary: "Public cert at {{ "{{" }} $labels.instance {{ "}}" }} expiring soon"
            description: "Serving cert < 14d from expiry."
        # Proactive Pi cert-sync health: auth.pmon.dev is Caddy/Pi-served, so its
        # served wildcard goes stale if the per-Pi caddy-wildcard-sync timer
        # stops propagating cert-manager's renewals. Healthy = always >28d
        # (renew ~30d before expiry + sync within a day); <25d ⇒ stalled, with
        # ~25d of runway — fires well before PublicSSLCertExpiringSoon (<14d).
        - alert: CaddyWildcardSyncStalled
          expr: (probe_ssl_earliest_cert_expiry{job="blackbox-public", instance=~"https://auth.pmon.dev.*"} - time()) / 86400 < 25
          for: 1h
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/CaddyWildcardSyncStalled"
            summary: "Pi Caddy serving a stale *.pmon.dev wildcard"
            description: "Cert served on auth.pmon.dev (Pi/Caddy) is <25d from expiry; a healthy renewal + caddy-wildcard-sync keeps it >28d. The per-Pi sync timer likely stalled (or cert-manager renewal failed — check CertManagerRenewalFailing). Re-run task deploy:caddy; Pi HTTPS lapses if left."

    - name: infra-certmanager
      rules:
        - alert: CertManagerRenewalFailing
          expr: certmanager_certificate_ready_status{condition="False"} == 1
          for: 1h
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/CertManagerRenewalFailing"
            summary: "cert-manager failed to renew {{ "{{" }} $labels.namespace {{ "}}" }}/{{ "{{" }} $labels.name {{ "}}" }}"
            description: "Certificate not Ready for 1h. ACME challenge, DNS-01 webhook, or CA issuer may be broken."
        - alert: CertManagerOrderFailing
          expr: increase(certmanager_http_acme_client_request_count{status=~"4..|5.."}[30m]) > 5
          for: 30m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/CertManagerOrderFailing"
            summary: "cert-manager ACME requests failing"

    - name: infra-prometheus
      rules:
        - alert: PrometheusTargetMissing
          expr: up == 0
          for: 5m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PrometheusTargetMissing"
            summary: "Target {{ "{{" }} $labels.job {{ "}}" }}/{{ "{{" }} $labels.instance {{ "}}" }} is down"
        - alert: PrometheusRuleFailures
          expr: increase(prometheus_rule_evaluation_failures_total[10m]) > 0
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PrometheusRuleFailures"
            summary: "Prometheus rule evaluation failing"
            description: "A rule is returning errors — invalid expression or missing series."
        - alert: PrometheusNotificationsFailing
          expr: rate(alertmanager_notifications_failed_total[10m]) > 0
          for: 10m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PrometheusNotificationsFailing"
            summary: "Alertmanager notifications failing ({{ "{{" }} $labels.integration {{ "}}" }})"
            description: "SMTP or webhook delivery is erroring. Check secrets, network, credentials."
        - alert: PrometheusScrapeDurationSlow
          expr: scrape_duration_seconds > 10
          for: 15m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/PrometheusScrapeDurationSlow"
            summary: "Slow scrape: {{ "{{" }} $labels.job {{ "}}" }}/{{ "{{" }} $labels.instance {{ "}}" }}"

    - name: ci-woodpecker
      rules:
        - alert: WoodpeckerBuildFailed
          # Woodpecker increments this counter when a pipeline ends in a
          # failure state. Any non-zero increase in the last 5min fires.
          expr: increase(woodpecker_pipeline_count{status="failure"}[5m]) > 0
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/WoodpeckerBuildFailed"
            summary: "Woodpecker build failed (repo {{ "{{" }} $labels.repo {{ "}}" }})"
            description: "A pipeline on {{ "{{" }} $labels.repo {{ "}}" }} ended in failure. Check ci.pmon.dev."
        - alert: WoodpeckerScrapeDown
          expr: up{job="woodpecker-server-metrics"} == 0
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/WoodpeckerScrapeDown"
            summary: "Cannot scrape Woodpecker metrics endpoint"
            description: "Build-failure alerts are silent until this resolves. Check the bearer token + the woodpecker-server-metrics Service."

    {{- if eq (.Values.smartctl.enabled | toString) "true" }}
    - name: infra-smartctl
      rules:
        # Drives in actual trouble — SMART self-test failure or NVMe critical
        # warning bitfield non-zero. Either of these means the firmware
        # itself flagged a problem; treat as page-worthy.
        - alert: SmartctlDeviceFailing
          expr: smartctl_device_smart_status == 0
          for: 5m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlDeviceFailing"
            summary: "SMART self-test reports failure on {{ "{{" }} $labels.device {{ "}}" }}"
            description: "smartctl_device_smart_status=0 — drive reports impending failure. Replace immediately."
        - alert: SmartctlNvmeCriticalWarning
          expr: smartctl_device_critical_warning > 0
          for: 5m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlNvmeCriticalWarning"
            summary: "NVMe critical warning on {{ "{{" }} $labels.device {{ "}}" }}"
            description: "Composite NVMe warning bitfield non-zero (spare low / temp / read-only / volatile-memory backup / persistent-memory unreliable). Inspect via 'nvme smart-log /dev/disk/by-id/{{ "{{" }} $labels.device {{ "}}" }}'."

        # Data integrity — one media error means the drive returned bad data
        # or detected corruption. Should be zero in steady state.
        - alert: SmartctlMediaErrors
          expr: smartctl_device_media_errors > 0
          for: 5m
          labels:
            severity: critical
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlMediaErrors"
            summary: "Media error on {{ "{{" }} $labels.device {{ "}}" }} ({{ "{{" }} $value {{ "}}" }} total)"
            description: "Drive's internal counter for unrecoverable read/write errors is non-zero. Check filesystem integrity, plan replacement."

        # Wear: 980 PRO has rated endurance of 600 TBW (1TB) → percentage_used
        # creeps up linearly. Alert at 80% so we have lead time to replace.
        - alert: SmartctlWearHigh
          expr: smartctl_device_percentage_used > 80
          for: 1h
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlWearHigh"
            summary: "{{ "{{" }} $labels.device {{ "}}" }} at {{ "{{" }} $value {{ "}}" }}% wear"
            description: "Drive endurance rating is {{ "{{" }} $value {{ "}}" }}% used. Plan replacement before 100%."

        # Power-cycle delta as proxy for unsafe shutdowns. The 980 PRO
        # firmware bug (1B2QGXA7) caused ~1 unsafe shutdown per 1.6h on
        # ten before the APST mitigation was applied. Normal operation:
        # power_cycle_count increments only on planned reboots — should
        # be a few per week, not per day.
        - alert: SmartctlExcessivePowerCycles
          expr: increase(smartctl_device_power_cycle_count[24h]) > 5
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlExcessivePowerCycles"
            summary: "{{ "{{" }} $labels.device {{ "}}" }} power-cycled {{ "{{" }} $value {{ "}}" }}× in 24h"
            description: "Likely host instability — recurring crashes / reboots. Cross-check kernel.softlockup_panic + pstore + 'last -x'. If APST mitigation lapsed, see project_ten_host_wedge memory."

        - alert: SmartctlExporterDown
          expr: up{job="smartctl-exporter"} == 0
          for: 10m
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/SmartctlExporterDown"
            summary: "smartctl_exporter scrape failing"
            description: "Drive-health alerts are silent until this resolves. SSH ten and check 'systemctl status smartctl_exporter'."
    {{- end }}
    {{- if eq (.Values.pushgateway.enabled | toString) "true" }}

    - name: dr-restore
      rules:
        - alert: RestoreVerificationFailing
          # task dr:drill pushes restore_verify_success on a passing run; the
          # pushgateway records push_time_seconds. Fire if the last successful
          # drill is older than 35 days (a monthly cadence won't false-alarm).
          # Silent before the first-ever drill (metric absent by design).
          expr: time() - max(push_time_seconds{job="dr-drill"}) > 35 * 24 * 3600
          for: 1h
          labels:
            severity: warning
          annotations:
            runbook_url: "https://runbooks.pmon.dev/RestoreVerificationFailing"
            summary: "Disaster-recovery restore not verified in over 35 days"
            description: "No successful DR drill (task dr:drill — Velero + CNPG restore on Vagrant) has recorded restore_verify_success; the last success is {{ "{{" }} $value | humanizeDuration {{ "}}" }} past the 35-day threshold. Run `task dr:drill` to re-validate the backup/restore path."
    {{- end }}
{{- end }}
