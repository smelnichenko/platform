{{- if eq (.Values.fluentbit.enabled | toString) "true" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "schnappy.fullname" . }}-fluentbit-config
  labels:
    {{- include "schnappy.fluentbit.labels" . | nindent 4 }}
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush             5
        Log_Level         warn
        Parsers_File      /fluent-bit/etc/parsers.conf
        storage.path      /fluent-bit/db/
        storage.sync      normal
        storage.checksum  off

    [INPUT]
        Name              tail
        Tag               kube.*
        Path              /var/log/containers/*.log
        Parser            cri
        DB                /fluent-bit/db/tail.db
        Mem_Buf_Limit     64MB
        storage.type      filesystem
        Skip_Long_Lines   On
        Refresh_Interval  10
        Rotate_Wait       5
        Exclude_Path      /var/log/containers/*k6-*

    [FILTER]
        Name              kubernetes
        Match             kube.*
        Kube_URL          https://kubernetes.default.svc:443
        Kube_CA_File      /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        Kube_Token_File   /var/run/secrets/kubernetes.io/serviceaccount/token
        Merge_Log         On
        Merge_Log_Key     log_parsed
        K8S-Logging.Parser On
        Annotations       Off
        Labels            Off
        Buffer_Size       1024KB

    [FILTER]
        Name              grep
        Match             kube.*
        Exclude           $kubernetes['container_name'] fluent-bit

    # Lift `log_parsed.{traceId,spanId}` (set by the kubernetes filter
    # when the container log line is JSON) onto the top-level record
    # so the ClickHouse output can map them to columns.
    [FILTER]
        Name              nest
        Match             kube.*
        Operation         lift
        Nested_under      log_parsed
        Add_prefix        log_parsed_

    [FILTER]
        Name              modify
        Match             kube.*
        Rename            log_parsed_traceId trace_id
        Rename            log_parsed_spanId  span_id
        # Anything else from the parsed JSON goes back into log_parsed_*
        # keys — drop them; they're noise for the column-only schema.
        Remove_wildcard   log_parsed_

    {{- if eq (.Values.clickhouse.enabled | toString) "true" }}
    # ClickHouse — canonical log store + analytics. The `http` plugin
    # posts JSONEachRow which the ClickHouse HTTP interface accepts
    # when the URI carries an INSERT query. Filesystem-backed retry
    # from [SERVICE] inherits to outputs.
    [OUTPUT]
        Name              http
        Match             kube.*
        Host              {{ include "schnappy.clickhouse.serviceName" . }}.{{ .Release.Namespace }}.svc
        Port              8123
        URI               /?database=logs&date_time_input_format=best_effort&query=INSERT+INTO+logs.podlogs+FORMAT+JSONEachRow
        Format            json_lines
        json_date_key     timestamp
        json_date_format  iso8601
        http_user         schnappy
        http_passwd       ${CLICKHOUSE_PASSWORD}
        tls               Off
        Retry_Limit       5
        Workers           2
    {{- end }}

    {{- if eq (.Values.openobserve.enabled | toString) "true" }}
    # OpenObserve — Discover-like UI. Independent retry from
    # ClickHouse; if one sink is slow/down the other still ingests.
    # The /_json endpoint expects a JSON array (Format json), not
    # NDJSON. Using json_lines here returns 400 "trailing chars".
    [OUTPUT]
        Name              http
        Match             kube.*
        Host              {{ include "schnappy.openobserve.serviceName" . }}.{{ .Release.Namespace }}.svc
        Port              5080
        URI               /api/default/podlogs/_json
        Format            json
        json_date_key     timestamp
        json_date_format  iso8601
        http_user         ${OPENOBSERVE_ROOT_USER_EMAIL}
        http_passwd       ${OPENOBSERVE_ROOT_USER_PASSWORD}
        tls               Off
        # Halve in-flight concurrency — OO's actix is pinned to
        # 4 workers, two fluent-bit workers stacked too many
        # connections under burst, tripping Istio 503s.
        Workers           1
        # gzip compress — smaller payloads, lower OO HTTP handler
        # CPU per request.
        compress          gzip
        # Don't drop chunks after a few retries; backoff and survive
        # OO restarts (it's a single replica).
        Retry_Limit       False
    {{- end }}

  parsers.conf: |
    [PARSER]
        Name        cri
        Format      regex
        Regex       ^(?<time>[^ ]+) (?<stream>stdout|stderr) (?<_p>[^ ]*) (?<log>.*)$
        Time_Key    time
        Time_Format %Y-%m-%dT%H:%M:%S.%L%z
{{- end }}
